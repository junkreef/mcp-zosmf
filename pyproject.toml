[project]
name = "mcp-zowe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.11.3",
    "requests>=2.32.5",
]

[project.optional-dependencies]
test = [
    "pytest>=8.4.2",
]

[tool.uv]
package = true